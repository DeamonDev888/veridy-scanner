[package]
name = "veridy_scanner"
version = "0.3.3"
edition = "2021"
authors = ["Veridy <contact@veridy.ca>"]
description = "Scanner de surface modulaire pour veridy.ca (DNS, Ports, HTTP headers, TLS)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4.6.7", features = ["derive"] }
regex = "1"
